# Normalize line endings in the repo; check out native on each platform.
# Without this, Windows CRLF churn shows every file as modified on other machines.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
